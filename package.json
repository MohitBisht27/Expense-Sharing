{
  "name": "expense-sharing-root",
  "version": "1.0.0",
  "description": "Root package.json for Render deployment",
  "scripts": {
    "install": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm start"
  }
}
